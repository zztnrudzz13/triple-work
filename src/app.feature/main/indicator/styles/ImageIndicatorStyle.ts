import styled from '@emotion/styled'

export const ImageIndicatorWrapper = styled.div`
  display: flex;
  gap: 0.5rem;
  img {
    width: 3.75rem;
  }
  .text-container {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0.5rem;
  }
`
